import React, { useState} from 'react';
import localStorage from 'local-storage';

const addFromDatabase = (props) => {

  return (
    <div>
      <p>Plants from the Database:</p>
    </div>
  )

}

export default addFromDatabase